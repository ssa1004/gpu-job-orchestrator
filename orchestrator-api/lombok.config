# Kotlin lombok plugin 이 Java Lombok 어노테이션을 인식하도록 — Java/Kotlin 혼재
# 컴파일 단계에서 Kotlin 측이 @Getter / @Builder 로 합성된 메서드를 볼 수 있게 한다.
# config.stopBubbling = true — 상위 디렉토리로 추가 lombok.config 탐색 안 함 (CI 안정).
config.stopBubbling = true
